# Run-owned file entries for #57

Two kinds of `CHANGELOG.md` change are below: one entry to append, and two in-place pointer replacements inside released entries. The replacements are part of this ticket's acceptance criteria — two of its seven citations live in `CHANGELOG.md` — and they quote their search strings verbatim, which is why this note file itself contains the string the ticket's grep looks for. It goes away with the note.

## CHANGELOG.md — entry to add

Under `## [Unreleased]` → `### Changed`:

- Every citation pointing into the implementation-plans directory from a file that outlives it is repointed or dropped, so retiring that directory breaks nothing (#57). The pointers move; nothing they argue does. `docs/adr/0018`'s Consequences and this file's own 0.6.0 entry now name `docs/release-procedure.md` §8, "The coordinated case", where #54 wrote the cross-repo ordering out; the 0.7.0 entry's three settled steps resolve to §7 and §8 alone, two of the three having landed in the same section; `docs/adr/0020` names the audit that carved out the half it closes rather than the file that recorded the carve-out, the validation order itself being ADR-0003's; and `docs/measurements/2026-08-19-successful-run.md` names plan 018 the way the sentence beside it already did, that plan having landed as #31 and ADR-0026. What made the removal costly was two records existing nowhere else, and the larger is now **ADR-0028**: the 2026-08-16 decision that a refused create discloses nothing about the occupying job — the evidence that every 429 anyone has met is the client's own sequencing rather than contention between administrators, the client's deliberate hard stop that never reads the body, the privacy analysis that keeps owner identity out and leaves ADR-0012's uniform refusal intact, the explicit finding that enriching the refusal would move no `api_version`, and the two triggers that would reopen it. It is that decision restated in an ADR's form, with none of its reasoning changed. `tests/Integration/extractions-test.php` cites the ADR where it used to cite the directory, which is the whole job of that comment: to say why the test asserts a bare ceiling refusal and nothing about the occupant. §10 of the release procedure no longer records inlining-and-repointing as an alternative considered and rejected, because it is what has since been done, and names where each record that would have been lost now lives. No REST change; no code change.

## CHANGELOG.md — two in-place pointer replacements

Neither is a reword: in both, only the citation target changes and the sentence around it is untouched. Each search string occurs exactly once in the file.

**1. In `## [0.7.0] – 2026-08-21`**, the `docs/release-procedure.md` bullet (line 77 on this branch). Replace

```
are now answered in §7, §8, and by tracking `plans/`, with
```

with

```
are now answered in §7 and §8, with
```

Two of the three questions now resolve to §8: "how the production site receives the update" always did, and "where Cross-repo release order lives" does since #54 wrote the ordering into that same section.

**2. In `## [0.6.0] – 2026-08-16`**, the "**`api_version` moves from 6 to 7**" bullet (line 90 on this branch). Replace

```
(`plans/README.md` → "Cross-repo release order")
```

with

```
(`docs/release-procedure.md` §8, "The coordinated case")
```

## CHANGELOG.md — deliberately left as it stands

The `## [Unreleased]` entry for #54 (line 23 on this branch) names the directory twice — "carried only by an execution record in `plans/`, a directory slated for removal", and "§10's record of why the ordering was left in `plans/` says what has since moved". Neither is a citation that has to resolve to a file: both name the directory as the subject of a sentence about its retirement, and both stay true once it is gone. §10 still records why the ordering was left there and what has since moved, so the second still resolves. Left alone on purpose, not overlooked.
