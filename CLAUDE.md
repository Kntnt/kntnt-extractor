@AGENTS.md

## Release configuration

- Version locations: the `Version:` header in `kntnt-extractor.php` (the only place outside `CHANGELOG.md`'s latest heading).
- Archive build: `./build-release-zip.sh` produces `kntnt-extractor.zip` (the exact asset name `Update_Checker::ASSET_NAME` expects on the GitHub release).
